# 最终项目重构计划（小驼峰命名）

## 1. 重构目标

* 优化项目目录结构，采用清晰的功能模块划分

* 修正组件引用路径错误

* 清理未使用的重复文件

* 优化代码组织和命名，views下使用小驼峰命名

## 2. 目录结构重构

### 原结构

```
src/
├── assets/
├── components/
│   ├── ChatMessages.vue
│   ├── DigitalHuman.vue
│   ├── DigitalHumanVideo.vue
├── hooks/
│   ├── useWebSocket.js
├── views/
│   ├── basic/
│   │   ├── chatPage/
│   │   │   ├── index.vue
│   │   ├── chatPage.vue
│   ├── chatMessages/
│   │   ├── index.vue
│   ├── digitalHuman/
│       ├── index.vue
├── App.vue
├── main.ts
```

### 新结构

```
src/
├── assets/             # 静态资源
├── components/         # 组件目录
│   ├── DigitalHumanVideo.vue  # 数字人视频组件
├── hooks/             # 自定义钩子
│   └── useWebSocket.js
├── views/             # 页面视图和组件
│   ├── chatPage/      # 主聊天页面（小驼峰）
│   │   └── index.vue
│   ├── chatMessages/  # 聊天消息组件（小驼峰）
│   │   └── index.vue
│   ├── digitalHuman/  # 数字人组件（小驼峰）
│       └── index.vue
├── App.vue
├── main.ts
```

## 3. 重构步骤

### 步骤 1：创建新的目录结构

* 创建 `views/chatMessages/` 目录（小驼峰）

* 创建 `views/digitalHuman/` 目录（小驼峰）

* 创建 `views/chatPage/` 目录（小驼峰）

### 步骤 2：移动和重命名文件

* 将 `components/ChatMessages.vue` 移动到 `views/chatMessages/index.vue`

* 将 `components/DigitalHuman.vue` 移动到 `views/digitalHuman/index.vue`

* 将 `views/basic/chatPage.vue` 移动到 `views/chatPage/index.vue`

### 步骤 3：删除重复文件

* 删除重复的 `views/basic/chatPage/index.vue`

* 删除重复的 `views/chatMessages/index.vue`

* 删除重复的 `views/digitalHuman/index.vue`

* 删除空的 `views/basic/` 目录

### 步骤 4：修正组件引用路径

* 更新 `views/chatPage/index.vue` 中 DigitalHuman 和 ChatMessages 组件的引用路径

* 更新 `App.vue` 中对 chatPage 组件的引用路径

### 步骤 5：保留必要文件

* 保留 `DigitalHumanVideo.vue` 组件在 `components/` 目录下

* 保持 `useWebSocket.js` 为 js 文件

### 步骤 6：测试重构后的项目

* 运行项目，确保所有功能正常

* 检查是否有编译错误

## 4. 预期效果

* 项目结构更加清晰，功能模块划分明确

* 组件引用路径正确

* 移除重复文件，减少项目体积

* 代码组织和命名更加规范，views下使用小驼峰命名

* 保留了所有必要的组件和文件

## 5. 注意事项

* 在移动文件前，确保先备份项目

* 重构过程中，注意保持组件间的依赖关系

* 确保所有引用路径都已更新

* 测试重构后的项目，确保所有功能正常运行

